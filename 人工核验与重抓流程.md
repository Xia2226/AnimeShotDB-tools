# 隔离条目人工核验与重抓流程

> 目标：处理构建时被隔离（quarantine）的条目——人工判断归属 → 修正题库 → 重抓正确图片 → 重新构建。
> 隔离报告：`resources/generated/anime-library-quarantine.json`（仅存档复核，不参与部署）。

***

## 0. 总体思路

```
隔离报告 → 人工判断归属 → 修正题库（删/改行）→ --override 重抓 → 重新构建
```

报告里每条记录都带 **`lineNumber`**（对应 `resources/fancaps_anime_images.jsonl` 中的行号）、标题、`show_url`、图片数，方便定位。

***

## 1. 隔离报告结构

```json
{
  "summary": { 构建统计 },
  "quarantine": {
    "crossAnidbShowUrls":    [259 组]  同一 FanCaps 剧集页被多个 AniDB ID 引用
    "duplicateAnidbIds":     [24 组]   同一 AniDB ID 绑定了多行
    "sharedImageIds":        [49 个]   同一图片 ID 被多部番剧引用
    "emptiedByImageCleanup": [0 条]    因删共享图被整部清空的（当前没有）
    "adultContent":          [82 部]   成人/擦边内容
  }
}
```

***

## 2. 分门别类核验

### ① duplicateAnidbIds（24 组）——优先处理，最容易判断

同一 AniDB ID 绑了多个 bgm 条目，多为**数据源映射错误**。

真实案例：

| anidb\_id | bgm\_ids                  | 问题             |
| --------- | ------------------------- | -------------- |
| 69        | 975《航海王》、1869《航海王:黄金岛大冒险》 | 剧场版误绑 TV 版 ID  |
| 77        | 3166《超时空要塞》、89875《太空堡垒》   | 完全不同的作品绑到同一 ID |

处理方式：打开 FanCaps 搜索页确认哪行绑错 → 在题库里**删除绑错的行**，或查出正确 ID 改掉。

### ② crossAnidbShowUrls（259 组）——工作量大，挑明显的做

同一 FanCaps 剧集页被多个 AniDB ID 引用。打开报告里的 `showUrl`，浏览器确认页面归属：

- **确认共页合理**（TV 与续作/OVA 共用一页，如《恋姬无双》/《真・恋姬无双》）→ 只能保留其中一部，删除另一行；
- **确认张冠李戴**（如《三叶草》和《黑色五叶草》共用 show 10817）→ 去 FanCaps 搜索正确作品，拿到它**自己的 show 页面链接**，修正题库里错行的 `show_url`。

### ③ adultContent（82 部）——抽查即可

按 subject 标签（里番、色情、R18、工口等）过滤，`仅卖肉/福利/杀必死`会保留。抽查几部确认无误杀，确属成人内容就不用处理。

### ④ sharedImageIds（49 个）——一般不用处理

被 120+ 部番剧共用的图片（如 imageId 42198）明显是 FanCaps 共用素材，剔除合理；当前未导致任何一部被清空，影响可忽略。

***

## 3. 修正题库

用编辑器打开 `resources/fancaps_anime_images.jsonl`，按 `lineNumber` 定位，三选一：

- **删行**：确认归属错误、无法补救的条目；
- **改** **`anidb_id`**：换成正确的 AniDB ID；
- **改** **`show_url`**：换成正确的番剧 show 页面链接。

> ⚠️ 改前务必手动备份，例如复制为 `fancaps_anime_images.before-fix.jsonl`。

***

## 4. 重抓正确页面（--override）

对改了 `show_url` 或想换图源的条目，用 `--override` 跳过搜索、直接抓指定页面：

**交互式**：双击 `start-retry.cmd` → 选 **10**（按 AniDB ID + 自定义番剧页链接）→ 按提示输入。

**命令行**（可一次多个，每个 `--override` 一条）：

```bash
node "tools\retry-fancaps-missing.mjs" --override 6400=https://fancaps.net/anime/showimages.php?6400-xxx --override 12665=https://fancaps.net/anime/showimages.php?12665-xxx
```

**关键限制：**

- 链接必须是**番剧 show 页**（`showimages.php?<id>-<标题>`），不是单集页或单张图片页；
- retry 脚本**只处理** **`status=not_found`** **或** **`status=error`** **的行**，`status=ok` 的行不会重抓；
- `--override` 里的 ID 必须等于题库该行的 `anidb_id`。

***

## 5. 重新构建与验证

```bash
start-build-library.cmd → 选 1（完整构建，含封面）
# 快速验证可选：选 2（只构建题库，不下载封面）
# 不联网校验可选：选 3
```

验证点：

- `anime-library-quarantine.json` 里对应条目是否消失；
- `finalAnimeCount` / 封面数是否回升；
- 与上次构建的 `summary` 对比。

***

## 6. 实操建议（按场景）

### 场景 A：只是映射错误、图片正确（多数 duplicateAnidbIds 组）

```text
1) 改 jsonl：把错误行的 anidb_id 改成正确 ID（或删除绑错的行）
2) 直接重新构建
```

**不用跑 retry**——图片本来就没错，改对 anidb\_id 后重建即恢复进库。

### 场景 B：show\_url 错、要换图源

```text
1) 改 jsonl：把该行 status 改成 error（或删掉 images、status 改为 not_found）
2) 跑脚本：retry --override <anidb_id>=<正确show页链接>
   ← 注意 override 的 ID 必须等于行里的 anidb_id
3) 重新构建
```

因为 retry 只看 `not_found/error`，**不改成这两种状态它不会重抓**；只改 `show_url` 不会自动换图。

### 场景 C：同页多部剧（crossAnidbShowUrls 组）

修 anidb\_id 也没用——**只要 show\_url 相同，重建时仍会被整组隔离**。必须二选一：

- 删除其中一行的记录；
- 或找到另一部剧**自己独立的 show 页**，改 `show_url` + 按场景 B 重抓。

***

## 7. 常见操作决策表

| 场景                        | 需要改 jsonl                     | 需要跑脚本                  | 说明                                                 |
| ------------------------- | ----------------------------- | ---------------------- | -------------------------------------------------- |
| 只改 `anidb_id`（映射错误，图片正确）  | 是                             | **否**                  | 改完直接重建即可进库                                         |
| 只改 `show_url`（要换图源）       | 是（含把状态改为 `error`/`not_found`） | **是**（`--override` 重抓） | 改 `show_url` 不会自动换图                                |
| 只跑脚本，不改 jsonl             | 否                             | 是                      | `ok` 行不变；`not_found/error` 行重试，但**不会修正 anidb\_id** |
| 同页多部剧（crossAnidbShowUrls） | 是                             | 视情况                    | 修 anidb\_id 没用，必须删行或换到独立 show 页                    |

***

